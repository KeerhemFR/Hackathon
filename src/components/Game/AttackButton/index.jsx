export default function AttackButton() {
  return (
    <div>
      <button
        className="attackBtn"
        type="submit"
        // onClick={() => setProgress(progress - clickPower)}
      >
        Attack
      </button>
    </div>
  );
}
